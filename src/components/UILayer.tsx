import React from 'react'
import styles from './UILayer.module.css'

const UILayer: React.FC = () => {
  return (
    <div className={styles.uiLayer}>
      {/* UI elements will go here - inventory, dialogue boxes, etc. */}
    </div>
  )
}

export default UILayer
